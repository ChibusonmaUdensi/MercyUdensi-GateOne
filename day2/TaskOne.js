const prompt = require("prompt-sync")
let scores = []
for (let count = 0; count < 10; count++){
	let score = parseInt(prompt("enter array element" + (index + 1) + ":));
int scoreElemnt = keyboardInput.nextInt();
score(count) = arrayElement;

}

