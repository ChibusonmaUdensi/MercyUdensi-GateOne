import datetime

def main():
	print("FLOW-APP")
	LMP= input("First Date of Last Menstrual Period")
	cycle_length = input("Cycle Length(dd/mm/yyyy)")

	LMP = datetime.datetime.strptime(LMP, "%d/%m/%Y")
	cycle = int(cycle)
	estimated_ovulation = cycle - 14
	safe_period = 
	ovulation_day = datetime.timedelta(first, days = estimated_ovulation_day)
	evaluation_date = LMP + ovulation_day
p